import { type ThemeConfig } from "antd";

const theme: ThemeConfig = {
  token: {
    colorPrimary: '#f7a400',
    fontSize: 14,
  },
  components: {
    Layout: {
      siderBg: "#F0F0F0",
    },
  },
};

export default theme;
